#include <stdio.h>
#include <stdbool.h>
#include <cs50.h>

int main(void)
{
    //IsPrime
    //GetNumbers
}

bool IsPrime(int initial_number, int final_number)
{
    for (int i = initial_number; i < final_number; i++)
    {
        int prime = 
    }
}